# 🔒 torch 관련 패키지 버전 강제 고정 - Poetry 간접 의존성 보호!
# 이 파일은 poetry나 pip가 CPU 버전으로 바꾸는 것을 절대 방지합니다.

torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# AI 라이브러리 버전 고정
transformers==4.55.3
huggingface_hub==0.34.4
FlagEmbedding==1.2.11
accelerate==1.10.0
sentence-transformers==5.1.0

# NumPy 버전 고정 (호환성)
numpy==1.26.2

# OpenAI 고정
openai>=1.101.0,<2.0.0

# 구문 분석기 고정
supar==1.1.4
stanza==1.10.1
nltk==3.9.1
protobuf==6.32.0

# 사용법:
# pip install -c constraints.txt [패키지명]
# poetry install --constraint constraints.txt (지원되면)
# pip install --no-deps openai  # 의존성 없이 안전 설치
